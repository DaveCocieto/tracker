{% extends 'base.html.twig' %}

{% block stylesheet %}
    <link href="{{ asset('/build/map.css') }}" rel="stylesheet" type="text/css" />
    <link href="{{ asset('/tomtom/map.css') }}" rel="stylesheet" type="text/css" />
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src='{{ asset('/tomtom/tomtom.min.js') }}'></script>
    <script src="{{ asset('/build/map.js') }}"></script>
{% endblock %}

{% block body %}
    <!--<div style="background-color: skyblue;height: 200px;">-->
    <div id="map" class="gmap">
    </div>

    <div style="background-color: #18171B; color: white">
        <div class="m-container m-container--responsive m-container--xxl m-container--full-height m-page__container">
            {% for route in routes %}
                <div style="padding: 4px 0; cursor: pointer;" class="route-item" data-route-id="{{ route.id }}">
                    {{ route.name }}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
